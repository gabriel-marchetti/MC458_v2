#pragma once
#include <vector>
#include <random>

std::vector<int> initialize_random_array(size_t size, int min = 0, int max = 100);
std::vector<int> initialize_random_vector(size_t s, int min = 0, int max = 100);